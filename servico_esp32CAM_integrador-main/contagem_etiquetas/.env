# URL do backend principal para receber confirmações de leitura ESP32
BACKEND_WEBHOOK_URL=http://localhost:4000/api/integrations/esp32/reading-confirmed

# Chave usada para autenticar o webhook no backend
BACKEND_API_KEY=changeme
